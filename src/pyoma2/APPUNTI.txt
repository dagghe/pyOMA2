DOMANDE

- la cartella temp_old si può eliminare?
- ancora non so come usare il method, a volte viene usato nel run, a volte nell'mpe
- capire se il Plot_StDiag e Plot_MIF vanno messi nel Single setup o negli algoritmi
- proposta per la Geometria
   - creare classe a parte Geometria con metodi 1 e 2 che sono indipendenti dagli algoritmi (salva i risultati internamente)
   - creare metodo in single setup che prende una istanza di geometria e la usa insieme agli algoritmi


--

COSA CAMBIA

1) tolte dataclass, messo pydantic Basemodel ovunque
2) unite diverse cose sotto famiglie di algoritmi:
    - uniti file famiglia FDD e SSI
    - uniti run parameter FDD e SSI
    - uniti result FDD e SSI
3) adesso i metodi run() devono tornare la classe Result dell'algoritmo,
   sarà poi il single setup a salvarla nella variabile .result dell'algoritmo
4) algoritmo può essere inizializzato senza o con runparameter, nel caso
   si possono settare dopo
5) bisogna solo importare l'algoritmo senza importare la classe diei run parameter
   perchè già bindata dentro l'algoritmo
6) aggiunti diagrammi generati automaticamente in ./docs/diagram/
7) in Sel_from_plot.py sistemato nell'initi l'uso di algo



--

DA SITEMARE

Nel codice ho messo in alcuni punti un commento con FIXME,
cercarlo in tutto il codice


--

PROBLEMI / DA FARE

L'argomento method per ssicov 
(che può essere o “cov_unb”, cov_bias”, “cov_matmul”) anche se viene 
passato all’ inst dell algoritmo non viene usato 
(viene salvato giusto nei run_params, ma non viene usato dal metodo)

Aggiungere griglia a canvas tk (bassa priorita)

Aggiungere linee verticali e orizzontali a canvas tk (bassa priorita)
